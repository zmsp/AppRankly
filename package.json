{
  "name": "apprankly",
  "version": "1.4.1",
  "private": true,
  "license": "AGPL-3.0-only",
  "scripts": {
    "start": "cd app && npm start",
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build:frontend",
    "build:frontend": "cd app && npm run build:frontend",
    "build:static": "cd app && npm run build:static",
    "build:demo": "cd app && npm run build:demo"
  }
}
